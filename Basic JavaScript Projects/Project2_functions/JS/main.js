function myFunction() { // Th function at wil use the += operator
    var sentence = "I am learning"; // creating the variable
    sentence += " a lot from this book!"; // using the += operator to add to the variable
    document.getElementById('concatenate').innerHTML = sentence; // using documant.getElementById()
}